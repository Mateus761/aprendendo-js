//01) Crie uma função que dado dois valores (passados como parâmetros) mostre no console a soma, subtração, multiplicação e divisão desses valores.

const valores = (a, b) => [a + b, a - b, a / b, a * b];
console.log(valores(2,2));